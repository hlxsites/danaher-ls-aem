version: 1
indices:
  default:
    include:
      - /us/en/**
    exclude:
      - '/fragments/**'
    target: /us/en/query-index.json
    properties:
      lastModified:
        select: none
        value: parseTimestamp(headers["last-modified"], "ddd, DD MMM YYYY hh:mm:ss GMT")
      title:
        select: head > meta[property="og:title"]
        value: replace(attribute(el, "content"),"| Danaher Life Sciences", "").replace(attribute(el, "content"),"| Danaher Lifesciences", "")
      image:
        select: head > meta[property="og:image"]
        value: match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      keywords:
        select: head > meta[name="keywords"]
        value: attribute(el, "content")
      readingTime:
        select: head > meta[name="readingtime"]
        value: attribute(el, "content")
      publishDate:
        select: head > meta[name="publishdate"]
        value: parseTimestamp(attribute(el, "content"), "ddd, DD MMM YYYY hh:mm:ss GMT")
      type:
        select: head > meta[name="template"]
        value: attribute(el, "content")
      brand:
        select: head > meta[name="brand"]
        value: attribute(el, "content")