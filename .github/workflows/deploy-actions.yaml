name: Deploy Converter

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: tools/actions/convert
    steps:
    - uses: actions/checkout@v3
#    - name: Use Node.js
#      uses: actions/setup-node@v3
#      with:
#        node-version: '18' 
#    - run: npm ci
#    - run: npm run lint
#    - run: npm run test
#    - run: npm run build
    - name: Deploy
      run: |
        curl https://github.com/apache/openwhisk-cli/releases/download/1.2.0/OpenWhisk_CLI-1.2.0-linux-amd64.tgz -o ./wsk.tgz
        tar -xzf ./wsk.tgz wsk
        ./wsk list 
      
